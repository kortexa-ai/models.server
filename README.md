# models.server

Model serving infrastructure for Kortexa. Each subdirectory is a self-contained model with its own venv, run/setup scripts, and service units.

See `api.server/models/README.md` for backend details and architecture docs.
